# Copiá este archivo a .env y reemplazá los valores localmente.
# .env está ignorado por Git; .env.example sí puede publicarse.

API_BASE_URL=https://api.example.com
API_TOKEN=replace-with-local-token
